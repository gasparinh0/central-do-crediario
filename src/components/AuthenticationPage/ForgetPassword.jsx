export default function ForgetPassword({ title, subtitle }) {
    return(
        <div>
            <h1 className='text-2xl font-bold'>{title}</h1>
            <p className="text-xs">{subtitle}</p>
        </div>
    )
}